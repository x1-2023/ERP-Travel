# ============================================================
# VietERP Platform — Cấu hình Môi trường / Environment Configuration
# Sao chép sang .env và cập nhật giá trị / Copy to .env and update values
# ============================================================

# ─── Ứng dụng / Application ───────────────────────────────
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# ─── Cơ sở dữ liệu / Database ─────────────────────────────
DATABASE_URL=postgresql://vierp:your-password@localhost:5432/vierp_dev

# ─── Redis / Bộ nhớ đệm ───────────────────────────────────
REDIS_URL=redis://localhost:6379

# ─── NATS JetStream / Hệ thống sự kiện ────────────────────
NATS_URL=nats://localhost:4222

# ─── Xác thực / Authentication ─────────────────────────────
# Keycloak SSO
SSO_URL=http://localhost:8080
SSO_REALM=vierp
SSO_CLIENT_ID=vierp-app
SSO_CLIENT_SECRET=your-client-secret

# JWT (dự phòng / fallback)
NEXTAUTH_SECRET=your-nextauth-secret-min-32-chars
NEXTAUTH_URL=http://localhost:3000

# ─── Cổng API / API Gateway (Kong) ────────────────────────
API_GATEWAY_URL=http://localhost:8000

# ─── Travel booking channel / Kênh booking du lịch ─────────
ANVOYAGES_API_BASE_URL=http://localhost:3000
ANVOYAGES_SERVICE_TOKEN=your-anvoyages-service-jwt
VIETERP_TRAVELOPS_CONTROL_TOKEN=your-erp-direct-control-token

# ERP back-office auth and CRM bridge
ERP_SESSION_SECRET=your-erp-session-secret-min-32-chars
ERP_CRM_BRIDGE_SECRET=your-erp-crm-bridge-secret-min-32-chars
ERP_AUTH_STORE_PATH=.deploy/erp-users.json
ERP_AUDIT_LOG_PATH=.deploy/erp-audit.jsonl
NEXT_PUBLIC_CRM_URL=http://localhost:3018
CRM_PUBLIC_URL=http://localhost:3018

# ─── Tích hợp AI / AI Integration ─────────────────────────
ANTHROPIC_API_KEY=your-anthropic-api-key

# ─── Email / SMTP ──────────────────────────────────────────
SMTP_HOST=smtp.your-domain.com
SMTP_PORT=587
SMTP_FROM="VietERP <noreply@your-domain.com>"

# ─── Lưu trữ / Storage ────────────────────────────────────
S3_BUCKET=vierp-files
S3_REGION=ap-southeast-1

# ─── Thị trường Việt Nam / Vietnamese Market ───────────────
# VNPAY_TMN_CODE=your-vnpay-code
# MOMO_PARTNER_CODE=your-momo-code
# GHN_TOKEN=your-ghn-token

# ─── Tuỳ chỉnh thương hiệu / Branding Override ────────────
# BRAND_NAME="Your Platform Name"
# BRAND_SHORT="YPN"
# BRAND_DOMAIN="your-domain.com"
