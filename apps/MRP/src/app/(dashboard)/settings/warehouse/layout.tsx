import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Cài đặt kho',
  description: 'Cài đặt và cấu hình kho hàng',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
