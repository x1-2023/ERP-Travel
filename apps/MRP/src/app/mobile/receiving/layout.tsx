import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Receiving',
  description: 'Mobile goods receiving and intake processing - Nhận hàng và xử lý nhập kho trên di động',
};

export default function MobileReceivingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
