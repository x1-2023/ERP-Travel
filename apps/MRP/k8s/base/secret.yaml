# =============================================================================
# VietERP MRP - KUBERNETES SECRET
# Sensitive configuration (values should be base64 encoded)
# Replace placeholders with actual base64-encoded values
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: vierp-mrp-secrets
  namespace: vierp-mrp
  labels:
    app: vierp-mrp
type: Opaque
stringData:
  # Database
  DATABASE_URL: "postgresql://user:password@postgres:5432/rtr_mrp?schema=public"
  
  # Redis
  REDIS_URL: "redis://redis:6379"
  
  # Auth
  NEXTAUTH_SECRET: "your-secret-key-minimum-32-characters"
  NEXTAUTH_URL: "https://app.vierp-mrp.com"
  
  # AWS S3
  AWS_ACCESS_KEY_ID: "your-access-key"
  AWS_SECRET_ACCESS_KEY: "your-secret-key"
  AWS_REGION: "ap-southeast-1"
  S3_BUCKET: "vierp-files-mrp"
  
  # Sentry (optional)
  SENTRY_DSN: ""
  
  # Email (optional)
  SMTP_HOST: ""
  SMTP_USER: ""
  SMTP_PASS: ""

---
# For production, use External Secrets Operator or Sealed Secrets
# Example with External Secrets:
# 
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: vierp-mrp-secrets
#   namespace: vierp-mrp
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: ClusterSecretStore
#   target:
#     name: vierp-mrp-secrets
#   data:
#     - secretKey: DATABASE_URL
#       remoteRef:
#         key: vierp-mrp/production
#         property: DATABASE_URL
