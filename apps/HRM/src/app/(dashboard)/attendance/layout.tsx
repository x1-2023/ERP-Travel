import type { Metadata } from "next"

export const metadata: Metadata = { title: "Chấm Công | VietERP HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
