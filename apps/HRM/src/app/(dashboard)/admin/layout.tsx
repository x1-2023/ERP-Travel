import type { Metadata } from "next"

export const metadata: Metadata = { title: "Quản Trị | VietERP HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
