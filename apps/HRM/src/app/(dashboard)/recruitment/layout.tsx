import type { Metadata } from "next"

export const metadata: Metadata = { title: "Tuyển Dụng | VietERP HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
