import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Đánh Giá Năng Lực | VietERP HRM",
}

export default function ReviewsLayout({ children }: { children: React.ReactNode }) {
  return children
}
