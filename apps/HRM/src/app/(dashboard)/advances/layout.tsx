import type { Metadata } from "next"

export const metadata: Metadata = { title: "Tạm Ứng | VietERP HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
