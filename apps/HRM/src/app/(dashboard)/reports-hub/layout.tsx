import type { Metadata } from "next"

export const metadata: Metadata = { title: "Báo Cáo Tổng Hợp | VietERP HRM" }

export default function ReportsHubLayout({ children }: { children: React.ReactNode }) {
  return children
}
