import type { Metadata } from "next"

export const metadata: Metadata = { title: "Biến Động Nhân Sự | VietERP HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
