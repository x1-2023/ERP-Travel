# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/rtr_hrm"

# NextAuth
NEXTAUTH_SECRET="your-secret-min-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# Anthropic (HR Copilot)
ANTHROPIC_API_KEY="sk-ant-..."

# SMTP (optional — emails skipped if not configured)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="VietERP HRM <hrm@vierp.com>"

# Cron job secret
CRON_SECRET="your-cron-secret"

# Storage (Render persistent disk — omit for local dev)
# RENDER_DISK_PATH="/data"

# App URL (production)
# NEXT_PUBLIC_APP_URL="https://rtr-hrm.onrender.com"
